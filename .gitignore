# Gradle / Android
.gradle/
build/
local.properties
captures/
*.apk
*.aab
*.keystore
!debug.keystore

# IDE
.idea/
*.iml
.DS_Store

# Secrets (API keys injected at build time — never commit)
secrets.properties
keys.xml
